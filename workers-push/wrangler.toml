name = "xjb-push-server"
main = "src/push-server.js"
compatibility_date = "2024-12-01"


[vars]
VAPID_SUBJECT = "mailto:patrickyzlu@gmail.com"

# Secrets (set via `wrangler secret put`):
#   VAPID_PRIVATE_JWK
#   VAPID_PUBLIC_KEY
#   ADMIN_TOKEN

[triggers]
crons = ["0 8 * * *"]  # daily 8am UTC = 16:00 Beijing
